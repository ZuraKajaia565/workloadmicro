# src/test/resources/cucumber.properties
# Don't specify cucumber.glue here - let each runner specify its own glue
cucumber.filter.tags=not @ignored

# Don't load all plugins in the classpath
cucumber.object-factory=io.cucumber.spring.SpringFactory